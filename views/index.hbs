{{> head}}
{{> navbar}}

{{#if message}}
  <div class="alert alert-{{message.type}}" role="alert">
    {{message.message}}
  </div>
{{/if}}

<div class="container mt-4">
    <div class="row">
        <div class="col">
            <h2>Hello World</h2>
        </div>
    </div>
</div>

{{> footer}}