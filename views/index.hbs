{{> head}}
{{> navbar}}

{{#if message}}
  <div class="alert alert-{{message.type}}" role="alert">
    {{message.message}}
  </div>
{{/if}}

<div class="container">
    <div class="row text-center m-5">
      <div class="col">
        <img src="../static/image/movie.jpg" style="max-width: 100%;">
      </div>
    </div>
    <div class="row text-center m-5">
        <div class="col">
            <h2>Welcome to Ticket App!</h2>
            <h3>Please login / register to using this app.</h3>
        </div>
    </div>
</div>

{{> footer}}